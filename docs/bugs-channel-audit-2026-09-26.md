# Bugs channel audit — September 26, 2026

Scope: the 17 reports visible in Discord forum `1548170688857112637`, including their replies. No other channels were investigated. Base revision: `304582f0015e367f1e8d6a0bb47dd327dcddb3c1` (.34).

The completed fixes are included in release .35. Release preparation and verification are recorded in `Verification/release-35-20260926/`. The ordinary desktop installation is not part of this deployment. No Discord messages, reactions, tags, or report statuses were changed.

## Report outcomes

| Report ID | Report | Outcome |
| --- | --- | --- |
| 1553375866946592888 | Invisible collision at Akina uphill start after abandoning/retrying | Reproduced a retained secondary actor colliding in a subsequent solo race. Solo setup now disables the body-pair solver; AI battles and explicit online contact retain their collision path. Regression passed. |
| 1553327616239345714 | China submissions require VPN | Unresolved. Current service uses a workers.dev hostname; a regional routing fix needs a reachable authorized hostname/host and affected-network validation. Asked whether a custom domain is available. Exact request error/Player.log is still needed. No proxy or TLS/security bypass added. |
| 1553078843873689700 | DS4/DS5 shoulder shifting blocked after pause while throttle held | Fixed pause-close masking of the canonical shift-up bit. Tested both mapped shoulders while holding confirm/throttle through resume using synthetic Unity devices and actual native input submission. Physical hardware not tested. |
| 1552825338008969316 | Meter placement, transparency, alignment and replay RPM | Recorded tuning now initializes replay engine/transmission and tach range. Tuned AE86 replay retains 10,500 RPM. Removed editor outlines; imported meter decoration can extend 25% beyond an edge while remaining reachable. Selected groups take drag priority over overlapping bounds. Wide authored meters now have a larger default fit, centered in bumper view and left in chase/natural views, with stock-minimap clearance. Previously moved/resized layouts retain their legacy anchor and scale; Reset adopts the new defaults. Compact dials keep their right anchor. This is a desktop layout policy based on authored proportions, not a claim of exact arcade camera placement. Live three-camera checks, six GPU previews, migration checks and Youmu edge/Cancel checks pass. Maou transparency, Miku alignment and Classic artwork were already fixed in .33/.34 and current checks pass. |
| 1553197675703312535 | Linux updater stopped working again | Reproduced Wine rejecting a legitimate Z: path inside drive_c, whose final handle resolves to C:. Long-name expansion can also fail for this valid spelling. Installer now permits only verified same-file Wine drive aliases, checks both ancestor chains, and canonicalizes containment/process comparisons. Links, overlapping game/session folders and altered files remain rejected. Ten Wine and 34 Windows installer/path cases pass. Bundled helper rebuilt. This establishes Wine 9.0 coverage, not every Proton version/distro or confirmation of the reporter's exact error. |
| 1552838802928566414 | Duplicate car labels and make grouping | Corrected S14, FD and GC8 variant labels, Roman-numeral Evo names and manufacturer grouping. All 35 numeric car IDs remain unchanged. Website tests pass; included in the .35 leaderboard deployment. |
| 1553008126482186461 | AUTO music only plays Speedy Speed Boy | AUTO now chooses among 12 Stage 3 race songs without consecutive automatic repeats. Presentation-only randomness is separate from driving RNG. Explicit built-in/custom choices preserved. |
| 1552980823815495680 | G923 binding greys out | Fixed reproduced generic-HID capture deadlock caused by an already-held latching selector. Its initial state is ignored until release; fresh controls and cancel/release guards remain active. Synthetic pedal/selector tests pass; physical G923 needs reporter confirmation. |
| 1552943755059925023 | Sadamine camera bumps during sharp turns | Unresolved. Original bumper/chase camera matches source reference across 360 frames each. Report's video is wet downhill AE86 Levin. Need affected replay/camera setting to separate road/body motion from camera behavior. No handling changes made. |
| 1552863046018273312 | Replay headlights do not illuminate road | Replays now project road lights from recorded player/opponent poses and light states, including backward seeking. Native geometry tests pass in both Akina directions. |
| 1552820808622153728 | D7/D8/Zero/Arcade speed colors missing | Bound 192 layers across 61 complete authored four-color families. Recovered source thresholds: 90/150/210 km/h. Double Ace only has a neutral source atlas, so its tint is reconstructed. High-speed rainbow uses presentation time; exact palette/rate is a reconstruction, not proven arcade parity. |
| 1552836221930176669 | Evo III misfire too quiet | Increased only PACK22 cue 7 by 6.02 dB after source voice synthesis. Original cue timing, triggering, other cue levels and Effects volume remain intact. Source PCM, concurrent-cue isolation and production mixer tests pass; concurrent-cue fixture peak 8,603/32,767 with no clipping. This is a deliberate desktop mix adjustment, not a source-parity restoration or a guarantee against clipping under every possible mix. |
| 1552815198119665805 | Shift warning too late | Full brightness now uses original transmission target (workingBase minus 500 RPM), fading over preceding 500 RPM. Four engine ranges verified; tuning/physics unchanged. |
| 1552731971145441290 | Keyboard/wheel buttons stop after one race | Fixed a reproduced global input lock when a controller changes while a wheel pedal/selector is held. Controller release protection is now separate from keyboard input, and unmapped latching selectors do not prevent recovery. Initially held generic menu fallback buttons cannot auto-confirm after reconnect. Synthetic regression plus production polling/pause tests pass. The reporter's exact post-race hardware failure still needs confirmation; the completed-race/second-race fixture itself did not reproduce it. |
| 1552675194852606002 | Multiplayer contact disappears for one race | Not reproduced. Native ON/OFF/ON tests pass with delayed, duplicated and stale packets; two Unity clients retain contact through race/rematch. Need failing replay/session log. No speculative contact changes made. |
| 1552613619269640242 | Akagi uphill online FPS drops to 40 | Not reproduced here. Two LAN clients averaged about 225/224 FPS at a 240 cap, 100 ms simulated RTT, 20 ms jitter and 3% loss. Short contact/return fixture on RX 9070 XT; not an Internet Steam race, full high-speed lap or affected hardware. No performance improvement claimed. |
| 1552488998130290810 | Tsuchisaka full/section results maps overlap | Already fixed in .32. Current regression passes 584 checks across all 18 condition map cycles. |

Thread links: `https://discord.com/channels/1548169613206884355/1548170688857112637/threads/<report ID>`.

## Verification

Ignored local evidence: `Verification/discord-bugs-20260926/`.

- `controls-final/report.json`: 162 checks through production polling/capture and pause gates, held-selector/pedal reconnect, independent keyboard recovery, and live bumper/chase/natural meter anchors. Synthetic input, no physical wheel validation or OnGUI capture. An earlier hidden capture attempt (`controls-reconnect`) timed out waiting for OnGUI; final run explicitly disables captures.
- `solo-collision-test.log`: 2,269 native driving ticks, retained-rival reproduction and explicit contact preservation.
- `replay-2/report.json`: 72 checks. Tuned AE86 limit 12,000 versus stock 7,500; recorded 10,500 RPM retained; 48 road-light vertices per car, backward seek and save preservation.
- `meter-checks.log`: 992 signal checks, all 87 meters / 930 GPU cases / 132,286 catalog checks, plus existing effects/alignment/transparency suites. Layout cache regenerated and verified.
- `hud-edge-final/report.json`: 14 actual standalone editor checks: Youmu bounds, both edge directions, reachability, move-back and Cancel. Full HUD-picker screenshot attempts (`hud-editor`, `hud-editor-batch`) could not obtain OnGUI repaint in a hidden window; not counted as successful visual tests. Focused geometry/lifecycle checks passed without screenshot claims.
- `meter-placement-gpu.log`: 1,848 meter signal/layout/migration checks. `meter-placement/` contains six actual rendered meter-only previews (Stuttgart, Wide, Double Ace; bumper/chase); Wide previews visually inspected. These are not screenshots of a race.
- `online-collision/online-collision-app.log`: 16,267 checks across six native races, collisions ON/OFF/ON, both slots, rollback and delayed/stale packets.
- `akagi-uphill-host/report.json`, `akagi-uphill-join/report.json`: 27,416 / 27,417 checks, natural finish, connected lobby return and second race, saves preserved. `akagi-profile-summary.json` records timing; maxima include diagnostic captures/transitions.
- `wine-alias-final/wine-report.json`: ten Wine 9.0 installer scenarios pass, including native C:, Z: resolving to C:, linked-file rejection through an alias and both containment directions across aliases. Earlier alias fixture reproduces failure.
- `windows-paths.json`, `windows-installer.json`: ten Windows path cases (including short names/junction rejection/additional game process) and 24 installer cases (including rollback, concurrent edits, protected files, cleanup and restart).
- `oneshot-pcm-test.log`: 3,340,183 checks including 2,546,290 exact default-gain PCM frames and independent concurrent-cue gain isolation. `--pcm-only` explicitly skips the absent external ARM priority-case fixture; the unchanged priority reference boundary is not claimed retested.
- `misfire-mix-test.log`: 59,508 exact existing mixer frames, plus race misfire routing, concurrent cue and Effects mute checks. `engine-mixer-test.log`: exact PCM across six simulation/render batch sizes and pause/restart checks.
- `camera-reference.log`: 16,568 bit-exact bumper/chase comparisons.
- `engine-reference.log`: 3,920 profiles, 10,800 integrated engine/contact/tire/queue frames and 379,973 comparisons, including backfire routing.
- `ta-results-test.log`: 584 map checks.
- `remaining-bugs-build.log`, `remaining-bugs-scripts.log`: final Unity staging player/script builds succeeded; source and staged native DLL hashes match. `audio-build.log`: native DLL and affected audio tests built. Leaderboard: 39 tests passed in the preceding pass. AUTO selector: 500 choices, all 12 songs, no consecutive repeats, explicit choices preserved.

Staging uses an existing local CHD for private tests only. It is ignored and is not a release asset. Release .35 rebuilds the player and exact submission policy together.
